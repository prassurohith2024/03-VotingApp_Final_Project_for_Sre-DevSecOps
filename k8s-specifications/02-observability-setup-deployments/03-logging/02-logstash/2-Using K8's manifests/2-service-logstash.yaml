apiVersion: v1
kind: Service
metadata:
  name: logstash-service
  namespace: kube-system
spec:
  type: ClusterIP  # Only accessible within the cluster
  selector:
    app: logstash
  ports:
    - name: beats
      protocol: TCP
      port: 5044
      targetPort: 5044
